import React from 'react';

const Footer = () => {
    return (
        <footer className="text-center py-4 mt-4 border-top">
            <p className="text-muted mb-0">Desarrollado por SaPa</p>
        </footer>
    );
};

export default Footer;